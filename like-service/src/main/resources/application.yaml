spring:
  application:
    name: like-service

  datasource:
    url: jdbc:h2:mem:service_db
    username: sa
    password: secret
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082
